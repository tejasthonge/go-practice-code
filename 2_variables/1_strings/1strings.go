package main

import (
	"fmt"
)
//A sequence of bytes (UTF-8 encoded text)
func main() {
	//in go cant suport the '' single cort we must declet the by the duble cort 

	var name string = "Tejas Thonge"
	//alse we use the
	var surname = "Thonge"
	//here we do not spesfiy thire type
	fmt.Println(name)
	fmt.Println(surname)
}
